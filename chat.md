# User
我想做一个可联机的网页斗地主游戏，可以吗，另外两个人也可以是真人也可以是ai的那种，如果是ai那就用ai

# LLM
完全可以做，而且这个项目刚好能合理消耗你的免费API额度——AI对手每一轮出牌决策都调用一次大模型，一局斗地主约30~50次调用，7个账号的配额完全能支撑多房间同时运行，还能顺便练手全栈实时联机开发。

### 一、整体技术架构
采用「**前端界面 + 后端权威服务 + AI决策层**」的三层架构，所有牌局逻辑在后端计算，避免前端作弊，AI调用统一由后端管理。

| 层级 | 技术选型推荐 | 核心职责 |
|------|--------------|----------|
| 前端 | Vue3 + Vite + CSS/Canvas 绘制扑克牌 | 界面渲染、用户操作、WebSocket实时通信、出牌动画 |
| 后端 | Python FastAPI + WebSocket | 房间管理、发牌逻辑、牌型校验、出牌轮次、胜负判定、状态同步 |
| AI层 | 对接商汤 DeepSeek V4 Flash / SenseNova 模型 | 根据手牌与局面输出出牌决策，由后端校验合法性后执行 |

### 二、核心模块拆解
#### 1. 牌局规则引擎（最基础，必须先做对）
这部分是纯算法，和联机/AI无关，建议先单独写好单元测试：
- **牌型识别**：覆盖单张、对子、三张、三带一/三带二、顺子（≥5张）、连对（≥3对）、飞机、炸弹、王炸、四带二等全部合法牌型
- **大小比较**：同牌型比点数，炸弹>普通牌型，王炸最大，点数顺序 大王>小王>2>A>K>Q>J>10>…>3
- **流程控制**：洗牌发牌→叫地主→出牌轮次→管牌/不出→胜负结算
- **服务端权威**：所有牌只存在后端，前端仅接收自己的手牌与公共出牌区数据，杜绝前端作弊

#### 2. 联机房间系统
- 房间号机制：创建房间生成6位数字房号，其他玩家输入房号加入
- 状态同步：玩家准备、叫地主、出牌、等待等状态变更，实时广播给房间内所有人
- 人数弹性：房间支持1~3人，空位自动由AI补位，真人加入可随时替换AI
- 断线重连：玩家掉线后保留位置30秒，重连后同步当前完整牌局状态

#### 3. AI对手实现（消耗额度的核心）
**核心原则**：大模型只负责决策，后端必须做二次合法性校验，不合法则回退到规则AI兜底，保证游戏不崩溃。

**调用流程**：
1. 后端组装结构化局面上下文：自身手牌、上家出牌、身份（地主/农民）、剩余牌数
2. 调用大模型，强制要求输出JSON格式
3. 后端解析出牌结果，校验牌型、点数、是否在手牌中
4. 合法则执行出牌；不合法则重试1次，仍失败则用简单规则AI兜底

**Prompt示例（适配DeepSeek JSON模式）**：
```
你是一名斗地主高手，当前轮到你出牌。
你的手牌：{hand_cards}
上家出牌：{last_play}（为空表示你是先手）
你的身份：{role}
合法牌型：单张、对子、三张、三带一、顺子(≥5张)、连对(≥3对)、飞机、炸弹、王炸。
规则：只能出手牌中的牌；必须压过上家同类型牌，点数更大；无法压牌则选择不出。
严格输出JSON，不要任何额外文字：
{"action": "play" | "pass", "cards": ["3", "A", "大王"]}
```

搭配 DeepSeek V4 Flash 的 `response_format: json_object` 模式，输出格式稳定性很高，基本不会出现解析失败。

### 三、分步落地路线（从易到难）
#### 阶段1：单机MVP（1~2天）
- 先实现本地规则引擎 + 前端界面，跑通完整一局
- 接入AI出牌逻辑，验证牌型校验与AI决策合理性
- 目标：人机对战能完整打完，AI出牌基本符合规则

#### 阶段2：联机基础版（2~3天）
- 后端接入WebSocket，实现房间创建、加入、退出
- 支持2个真人 + 1个AI，实时同步出牌与状态
- 加入基础出牌动画、回合提示

#### 阶段3：体验完善（可选）
- 多难度AI：不同模型/不同Prompt对应简单、普通、高手三档
- 聊天功能、战绩统计、残局挑战模式
- 旁观模式、邀请链接

### 四、额度消耗与优化
- 单局游戏AI出牌约30~40次，按单账号每5小时150次DeepSeek额度计算，单账号5小时可支撑约4局，7个账号并行可以同时开多桌，额度充足
- 多余额度可用来批量生成斗地主残局题库，做成残局挑战模式，进一步消耗配额
- 直接复用你现有的多账号滑动窗口调度逻辑，自动轮询7个Key，均匀消耗额度

### 五、关键注意点
1. **永远不信任前端**：所有出牌合法性、胜负判定必须后端计算，前端只做渲染，防止改包作弊
2. **AI必须有兜底**：大模型偶尔会出非法牌，一定要写一个贪心规则AI作为兜底，保证游戏流畅
3. **控制并发**：API调用有延迟，给AI加「思考中」动画，避免玩家觉得卡顿

需要我先给你一份**牌型判断核心函数 + AI调用最小Demo**的可运行Python代码，让你先跑通单机人机对战吗？